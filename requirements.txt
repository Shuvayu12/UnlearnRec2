numpy>=1.23
scipy>=1.9
networkx>=2.8
setproctitle>=1.3

# Install torch separately in environments that need custom CUDA wheels.
# Kaggle usually provides torch already.
torch>=2.0

# torch-sparse is required by this project and may need a version-specific wheel.
# See README for Kaggle installation cell.
torch-sparse>=0.6
